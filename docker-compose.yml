version: '3.8'

services:
  chatbot-api:
    build: .
    container_name: chatbot-fastapi
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_db:/app/chroma_db
      - ./docs:/app/docs
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
